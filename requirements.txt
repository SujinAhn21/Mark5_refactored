# Mark5 (Mark4와 공용) 로컬 CPU 실행 환경 — Python 3.8
#
# 설치 방법 (CPU torch는 PyTorch 인덱스에서 받음):
#   python -m venv .venv
#   .venv/bin/python -m pip install -U pip wheel setuptools
#   .venv/bin/python -m pip install -r requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cpu
#
# 주의:
#  - sentence-transformers는 2.2.2 금지(huggingface_hub의 cached_download 제거로 import 실패).
#    2.7.0 사용. transformers/huggingface-hub 핀은 그 호환을 고정하기 위함.
#  - GPU 학습은 Colab에서 수행. 로컬 venv는 검증/소규모 실행용 CPU 빌드.

# --- PyTorch (CPU 빌드: +cpu 로컬 버전으로 CPU 휠 강제) ---
torch==2.4.1+cpu
torchaudio==2.4.1+cpu

# --- 텍스트 임베딩(ViLD text head) ---
sentence-transformers==2.7.0
transformers==4.46.3
huggingface-hub==0.36.2

# --- 오디오 처리 ---
librosa==0.10.2.post1
soundfile==0.12.1
pydub==0.25.1

# --- 수치/표/평가/시각화 ---
numpy==1.24.4
scipy==1.10.1
pandas==2.0.3
scikit-learn==1.3.2
matplotlib==3.7.5
seaborn==0.13.2
tqdm==4.66.5

# --- 효율성 프로파일링(FLOPs 측정, model/profile_efficiency.py) ---
thop==0.1.1.post2209072238
